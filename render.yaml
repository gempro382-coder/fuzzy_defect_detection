services:
  - type: web
    name: fuzzy-defect-detection
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd web && gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
